# Совместимость с Dify

## Анализ совместимости

### Текущий формат API

Ваш reranker API использует следующий формат:

**Запрос:**
```json
{
  "query": "поисковый запрос",
  "passages": ["документ 1", "документ 2", ...],
  "top_k": 5
}
```

**Ответ:**
```json
{
  "results": [
    {
      "index": 0,
      "document": "текст документа",
      "score": 8.234
    }
  ]
}
```

### Требования Dify для reranker

Dify использует reranker для улучшения результатов поиска в RAG (Retrieval-Augmented Generation) приложениях. Обычно Dify ожидает reranker API со следующими характеристиками:

1. **Формат запроса**: `query` + `documents`/`passages`
2. **Формат ответа**: Отсортированные результаты с оценками релевантности
3. **HTTP метод**: POST
4. **Content-Type**: application/json

## ✅ Совместимость

### Положительные моменты:

1. **Стандартный REST API** - Ваш API использует стандартный REST формат, который легко интегрируется
2. **Правильная структура данных** - Формат запроса (`query` + `passages`) соответствует ожиданиям Dify
3. **Корректный формат ответа** - Результаты отсортированы по релевантности с оценками
4. **Health endpoint** - Наличие `/health` эндпоинта полезно для мониторинга

### Возможные несоответствия:

1. **Название поля**: Dify может ожидать `documents` вместо `passages`
2. **Формат ответа**: Dify может ожидать другой формат ответа (например, без поля `index`)
3. **Эндпоинт**: Dify может ожидать другой путь (например, `/v1/rerank`)

## Рекомендации для интеграции с Dify

### Вариант 1: Прямая интеграция (если формат совместим)

Если Dify поддерживает кастомные reranker API, вы можете использовать ваш API напрямую:

1. Настройте ваш reranker как внешний сервис в Dify
2. Укажите URL вашего API: `http://your-server:8009/rerank`
3. Проверьте, что формат данных соответствует ожиданиям Dify

### Вариант 2: Адаптация API (рекомендуется)

Для полной совместимости можно добавить альтернативный эндпоинт, который соответствует формату Dify:

**Добавить в `app.py`:**
```python
class DifyRerankRequest(BaseModel):
    query: str
    documents: List[str] = Field(..., alias="documents")
    top_n: int = Field(default=5, ge=1)

class DifyRerankResult(BaseModel):
    document: str
    score: float

class DifyRerankResponse(BaseModel):
    results: List[DifyRerankResult]

@app.post("/v1/rerank", response_model=DifyRerankResponse)
async def dify_rerank(request: DifyRerankRequest):
    # Используем существующую логику, но адаптируем формат ответа
    top_k = min(request.top_n, len(request.documents))
    pairs = [[request.query, doc] for doc in request.documents]
    
    # ... существующая логика ранжирования ...
    
    scored = [
        DifyRerankResult(
            document=request.documents[i],
            score=float(scores[i].item())
        )
        for i in range(len(request.documents))
    ]
    scored.sort(key=lambda x: x.score, reverse=True)
    
    return DifyRerankResponse(results=scored[:top_k])
```

### Вариант 3: Прокси-слой

Создать тонкий прокси-слой, который преобразует запросы Dify в формат вашего API и обратно.

## Проверка совместимости

### Шаги для проверки:

1. **Проверьте документацию Dify** на предмет требований к reranker API
2. **Протестируйте интеграцию** с тестовым запросом
3. **Проверьте логи** на наличие ошибок формата данных

### Тестовый запрос для проверки:

```bash
curl -X POST http://localhost:8009/rerank \
  -H "Content-Type: application/json" \
  -d '{
    "query": "test query",
    "passages": ["document 1", "document 2"],
    "top_k": 2
  }'
```

## Выводы

### ✅ Ваш API **совместим** с Dify при условии:

1. Dify поддерживает кастомные reranker API
2. Формат данных соответствует ожиданиям Dify (возможно, потребуется небольшая адаптация)
3. API доступен из сети, где работает Dify

### ⚠️ Возможные действия:

1. **Проверьте документацию Dify** для точных требований к формату API
2. **Добавьте альтернативный эндпоинт** с форматом, специфичным для Dify (если требуется)
3. **Протестируйте интеграцию** в тестовой среде перед продакшеном

### 📝 Рекомендация:

Ваш API использует стандартный и логичный формат, который должен быть совместим с большинством систем, включая Dify. Для полной уверенности рекомендуется:

1. Проверить официальную документацию Dify по интеграции reranker
2. Протестировать интеграцию
3. При необходимости добавить альтернативный эндпоинт с форматом Dify

## Дополнительные ресурсы

- [Dify Documentation](https://docs.dify.ai/)
- [Dify GitHub](https://github.com/langgenius/dify)
- [Dify Reranker Integration Guide](https://docs.dify.ai/guides/knowledge-base/rerank)

